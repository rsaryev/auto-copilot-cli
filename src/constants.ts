export const CREATE_CLI =
  'Create 0-10 commands for the AI system which works with the command line to achieve the goal of {goal}.';
export const ANSWER_ONLY_JSON_PARSEABLE_ARRAY_OF_STRINGS =
  'Answer only as a JSON-parseable array of commands for example: ["git add .", "git commit -m \'Add changes\'", "git push"]. Use commands that can be executed automatically example: npm init -> npm init -y';
export const REPHRASE_GOAL =
  'Rephrase the following of {goal} as a goal for the AI system which works with the command line.';
export const ANSWER_ONLY_STRING =
  'Answer with only one line. For example: add changes to git branch';
